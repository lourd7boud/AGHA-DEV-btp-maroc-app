# دليل سريع - إعداد نظام التحديث التلقائي

## الخطوة 1: إنشاء GitHub Repository

```bash
# أنشئ repository جديد على GitHub
# مثال: https://github.com/YOUR_USERNAME/btp-maroc-app
```

## الخطوة 2: تعديل إعدادات النشر

افتح `frontend-electron/package.json` وعدّل:

```json
{
  "build": {
    "publish": {
      "provider": "github",
      "owner": "YOUR_GITHUB_USERNAME",  // ⚠️ غيّر هذا
      "repo": "btp-maroc-app"           // ⚠️ غيّر هذا
    }
  }
}
```

## الخطوة 3: إنشاء GitHub Token

1. اذهب إلى: https://github.com/settings/tokens
2. اضغط "Generate new token" → "Generate new token (classic)"
3. اختر Scopes:
   - ✅ `repo` (كامل)
4. انسخ الـ Token

## الخطوة 4: إعداد المتغيرات البيئية

### Windows:
```powershell
# مؤقت (للجلسة الحالية)
$env:GH_TOKEN="ghp_your_token_here"

# دائم
[System.Environment]::SetEnvironmentVariable('GH_TOKEN', 'ghp_your_token_here', 'User')
```

### Linux/Mac:
```bash
export GH_TOKEN="ghp_your_token_here"

# للإضافة بشكل دائم في ~/.bashrc أو ~/.zshrc
echo 'export GH_TOKEN="ghp_your_token_here"' >> ~/.bashrc
```

## الخطوة 5: بناء ونشر الإصدار الأول

```bash
# اذهب إلى مجلد Electron
cd frontend-electron

# تأكد من رقم الإصدار في package.json
# "version": "1.0.0"

# بناء التطبيق مع النشر
npm run build:win -- --publish always
```

سيتم:
- ✅ بناء التطبيق
- ✅ إنشاء ملفات التثبيت
- ✅ إنشاء GitHub Release تلقائياً
- ✅ رفع الملفات للـ Release

## الخطوة 6: إصدار تحديث جديد

```bash
# 1. عدّل رقم الإصدار في package.json
"version": "1.0.1"  # أو 1.1.0 أو 2.0.0

# 2. اعمل commit للتغييرات
git add .
git commit -m "Release v1.0.1: إضافة ميزات جديدة"

# 3. أنشئ tag
git tag v1.0.1
git push origin v1.0.1

# 4. انشر التحديث
npm run build:win -- --publish always
```

## الخطوة 7: اختبار التحديث

1. ثبّت الإصدار القديم (v1.0.0)
2. شغّل التطبيق
3. انتظر 3 ثواني
4. يجب أن يظهر إشعار بوجود تحديث (v1.0.1)
5. اضغط "تحميل التحديث"
6. انتظر اكتمال التحميل
7. اضغط "إعادة التشغيل والتثبيت"

## أوامر سريعة

```bash
# بناء بدون نشر
npm run build:win

# بناء ونشر
npm run build:win -- --publish always

# بناء لأنظمة أخرى
npm run build:mac    # macOS
npm run build:linux  # Linux

# تشغيل في وضع التطوير (بدون تحديثات)
npm run dev
```

## ملاحظات مهمة

### التحكم في نوع التحديث

| الإصدار القديم | الإصدار الجديد | النوع |
|----------------|----------------|-------|
| 1.0.0 | 1.0.1 | Patch (إصلاحات) |
| 1.0.0 | 1.1.0 | Minor (ميزات جديدة) |
| 1.0.0 | 2.0.0 | Major (تغييرات كبيرة) |

### التحقق من الإصدار المثبت

في التطبيق:
```typescript
const version = await window.electron?.getAppVersion();
console.log('Current version:', version);
```

### إلغاء نشر إصدار معيّن

```bash
# حذف release من GitHub
gh release delete v1.0.1

# أو من واجهة GitHub
# https://github.com/YOUR_USERNAME/btp-maroc-app/releases
```

## استكشاف الأخطاء السريع

### ❌ خطأ: "Cannot find GitHub token"
**الحل**: تأكد من إعداد `GH_TOKEN` في المتغيرات البيئية

### ❌ خطأ: "Repository not found"
**الحل**: تحقق من اسم المستخدم واسم الـ repo في package.json

### ❌ لا يظهر إشعار التحديث
**الحل**: 
1. تأكد من أن الإصدار الجديد أعلى من القديم
2. تحقق من وجود الإصدار الجديد في GitHub Releases
3. تأكد من أن التطبيق ليس في وضع Development

### ❌ خطأ في التثبيت
**الحل**: 
1. شغّل التطبيق كمسؤول (Admin)
2. أغلق برامج مكافحة الفيروسات مؤقتاً
3. تأكد من وجود مساحة كافية على القرص

## أمثلة Release Notes

عند عمل release، أضف وصف للتحديثات:

```markdown
## v1.0.1 - التحديثات الأمنية

### إصلاحات
- إصلاح مشكلة المزامنة مع السيرفر
- تحسين أداء قاعدة البيانات

### تحسينات
- إضافة إشعارات للتحديثات
- واجهة مستخدم محسّنة

### معلومات
- حجم التحديث: 45 MB
- وقت التثبيت: ~2 دقيقة
```

---

**بالتوفيق! 🚀**

إذا واجهت أي مشكلة، راجع الملف الكامل: `docs/AUTO_UPDATE_SYSTEM.md`
